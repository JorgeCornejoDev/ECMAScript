// ES7 -> 2016

// Algunos cambios fue agregar el valor include y crear operaciones de forma exponencial

// valor include
let numbers = [1,2,3,6,7,8,10];

if ( numbers.includes(9) ) {
    console.log( 'Si se encuentra el valor 7' );
}else {
    console.log( 'No se encuentra ese número' );
}

// Elevar a la potencia
 let base = 3;
 let exponent = 2;

 let result  = base ** exponent;
 console.log( result );

